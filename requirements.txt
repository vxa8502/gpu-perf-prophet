# Production runtime deps only (see requirements-dev.txt for pytest/pytest-cov/httpx, which must not ship). Pinned exact versions incl. the full resolved transitive closure (not just direct packages) for bit-reproducible builds; resolved 2026-07-11 against python:3.11-slim via `docker run --rm -v "$(pwd)/requirements-runtime.in:/tmp/req.in:ro" python:3.11-slim bash -c "pip install -r /tmp/req.in && pip uninstall -y nvidia-nccl-cu12 && pip freeze"` — regenerate the same way after bumping any direct pin below.

# Direct/top-level dependencies — data processing
pandas==3.0.3
numpy==2.4.6
pyarrow==25.0.0        # Parquet I/O
PyYAML==6.0.3           # GPU/LLM spec databases

# ML — lightgbm/optuna/mlflow were planned but never wired in (XGBoost was the model choice); removed 2026-07-11 after confirming zero imports anywhere in src/, notebooks/, tests/, benchmarks/.
scikit-learn==1.9.0
xgboost==3.2.0
shap==0.51.0

# API + serving
fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic==2.13.4

# UI
streamlit==1.59.1

# Process supervision — restarts uvicorn if it dies mid-life (docker/supervisord.conf); no transitive deps
supervisor==4.3.0

# Pinned transitive dependencies (resolved, not hand-picked). nvidia-nccl-cu12 is deliberately NOT listed here: xgboost's Linux wheel unconditionally pulls it in (~300 MB, CPU use never needs it — tree_method="hist" only) but the Dockerfile installs it as an unavoidable resolver side effect and uninstalls it in the same layer so it never ships.
altair==6.2.2
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.1
attrs==26.1.0
blinker==1.9.0
cachetools==7.1.4
certifi==2026.6.17
charset-normalizer==3.4.9
click==8.4.2
cloudpickle==3.1.2
gitdb==4.0.12
GitPython==3.1.50
h11==0.16.0
httptools==0.8.0
idna==3.18
itsdangerous==2.2.0
Jinja2==3.1.6
joblib==1.5.3
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
llvmlite==0.48.0
MarkupSafe==3.0.3
narwhals==2.23.0
numba==0.66.0
packaging==26.2
pillow==12.3.0
protobuf==7.35.1
pydantic_core==2.46.4
pydeck==0.9.3
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
python-multipart==0.0.32
referencing==0.37.0
requests==2.34.2
rpds-py==2026.6.3
scipy==1.17.1
six==1.17.0
slicer==0.0.8
smmap==5.0.3
starlette==1.3.1
tenacity==9.1.4
threadpoolctl==3.6.0
toml==0.10.2
tqdm==4.68.4
typing-inspection==0.4.2
typing_extensions==4.16.0
urllib3==2.7.0
uvloop==0.22.1
watchdog==6.0.0
watchfiles==1.2.0
websockets==16.1
